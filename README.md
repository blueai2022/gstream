# go_streaming
Demo project for modern bidirectional streaming backend in Golang

## gstream

gstream replicates NATS.io JetStream functionalities with a configurable underlying tech stack, e.g. Kafka + Redis (for persistent JetStream with KV Target Registry) and other common corporate infrastructure choices.